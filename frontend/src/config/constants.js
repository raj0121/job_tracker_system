export const DEFAULT_PAGE_SIZE = 10;
export const DEFAULT_STALE_TIME = 60 * 1000;
export const DEFAULT_GC_TIME = 10 * 60 * 1000;

export const DEFAULT_LIST_FILTERS = Object.freeze({
  search: "",
  status: "",
  priority: "",
  company_id: "",
  stage: ""
});
